apiVersion: v1
kind: Service
metadata:
  name: {{ include "circle1-port-zenodo.fullname" . }}
  labels:
    {{- include "circle1-port-zenodo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "circle1-port-zenodo.selectorLabels" . | nindent 4 }}
