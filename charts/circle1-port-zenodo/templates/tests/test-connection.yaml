apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "circle1-port-zenodo.fullname" . }}-test-connection"
  labels:
    {{- include "circle1-port-zenodo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "circle1-port-zenodo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
