apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "circle3_project_manager.fullname" . }}-test-connection"
  labels:
{{ include "circle3_project_manager.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "circle3_project_manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
